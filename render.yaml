services:
  - type: web
    name: indautor-chatbot
    env: node
    region: oregon
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
